Issue
- Chrome is very slow 
  - Use firefox

- Always get CAPTCHA and fail to pass
  - Real Firefox User-Agent (Fixed the "I am Chrome" lie that caused CAPTCHAs).
  - Persistent Profile (Saves your login so you don't get CAPTCHAs after the first run).
